# Editor / local tooling
.claude/

# Node / Vite
node_modules/
dist/
.vercel/
*.log
.DS_Store

# Local reference assets in this working folder — NOT part of the site.
# (The site's real assets live under public/assets and ARE tracked.)
/Gallery/
/Memories/
/DEMOS/
/PHOTOS/
/DOCUMENTS/
/INSTAGRAM/
/JOURNAL ENTRY/
/Logo variations/
/August Blu Design System*/
/TINTED WINDOWS*/
/media/
hf_*.png
white look*.png
Screenshot*.png
/*.png
*.zip
/August Blu - Campaign Film*.html

# Stray raw media dumped in the project root (audio/video sessions).
# Site media lives under public/assets, which is re-included below.
*.wav
*.mov
*.aif
*.aiff
!public/assets/**

# stray root SVGs (site assets live in public/assets)
/*.svg

# stray root media
/*.mp4

# source/reference files kept locally (their content ships via index.html + src/)
/landing.html
/WHAT IS ANTI POP.txt

# stray root text drops (content is embedded in the build)
/*.txt

# stray local video/source drops (not part of the site build)
/VISUAL MOVING/
*.mp4
!public/assets/**
